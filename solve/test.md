03/05 12:37

初步个人思考：题目的二维平面中，应该先根据坐标绘制出来大致的内容，并计算面积，同时要减去内开门所占用的面积。物品分类整理后，优先放置冰箱（限制较多），随后依次从墙边方向开始按面积从大到小放置，循环去尝试长边和宽边，然后对于剩下的面积再次进行处理循环尝试放置即可。

优先考虑：建立物品类，门类，轮廓类等，将信息保存进去，然后创建函数：是否在轮廓内函数，两点之间距离函数，点中心点函数，尝试放置函数等

初步想法建立函数-python
实现：Point类，Item类，Boundary类并实现方法（判断点是否在轮廓内），测试example后可正确读取
思路断开，寻找AI大模型寻求帮助

03/05 16:02

使用Deepseek模型，开始初步理解思路逻辑
1. 首先创建必要的几何计算功能：  
 1.1 获取物品矩形的四个点坐标   
 1.2 判断该物品矩形是否完全在多边形内  
 1.3 判断两个物品矩形是否重叠（采用分离轴定理）  
2. 扩展Item类，添加布局相关的属性  
 2.1 设置优先级（先放置谁）
 2.2 获取物品占用的尺寸区域
 2.3 获取禁止放置的区域
3. 扩展Boundary类，添加布局相关的属性  
 3.1 计算门所占的区域  
 3.2 生成沿墙的候选放置位置  
4. 实现核心布局算法  
 4.1 初始化禁止的区域  
 4.2 检查位置是否有效  
  4.2.1 检查是否在边界内  
  4.2.2 检查是否与禁止区域重叠  
  4.2.3 检查是否与已放置物品重叠  
  4.2.4 特殊物品约束规范  
 4.3 检查冰箱开门侧区域是否阻挡  
 4.4 寻找物品的放置位置  
 4.5 布局规划  

尽量独立完成一些简单函数
首先创建几何计算的工具类并设置静态函数
仅仅完成了获取旋转矩形的四个角坐标

